import React from 'react'
import './input-column.styles.scss'

const InputColumn = ({children}) => {
  return (
    <div className="input-column
    ">{children}</div>
  )
}

export default InputColumn